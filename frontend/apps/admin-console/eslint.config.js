// The shared React rules, unchanged. Rules live in packages/config so all three
// front ends stay consistent — add or change a rule there, not here.
import react from '@trips/config/eslint.react';

export default react;
